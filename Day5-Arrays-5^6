#include <bits/stdc++.h> 
void sort012(int *arr, int n)
{
   vector<int> v(n,-1);
   for(int i=0;i<n;i++){
      v[i] = arr[i];
   }
   sort(v.begin(),v.end());

   for(int i=0;i<n;i++){
      arr[i]=v[i];
   }
}
